#include <iostream>
#include <ctime>

using namespace std;

void maxmin(int vetor[], int n, int &maximo, int &minimo) {
}

void imprimir(int vetor[], int tamVetor) {
}

int main() {
    /*
        1- Foi percebido que na função/procedimento maxmin há uma alta coesão quando falamos de encontrar o valor máximo e mínimo, pois, se trata 02 elementos em busca
        da mesma tarefa;
        2- Já em relação ao acoplamento foi entendido que as 02 funções(procediemntos) possui um grau de dependência baixo, logo possuem baixo acoplamento. 
    */

    return 0;
}